#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin >> t;
  for(int i = 0; i < t; ++i) {
    int n, alice, goal;
    cin >> n >> alice >> goal;
    int bob = alice + 3;
    vector<int> arr(n);

    for(int j = 0; j < n; ++j) {
      cin >> arr[j];
    }

    

  }
  
  return 0;
}